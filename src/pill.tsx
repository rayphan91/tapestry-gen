import React from 'react';
import ReactDOM from 'react-dom/client';
import { VideoEditor } from './components/SocialPill';
import './styles/global.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VideoEditor />
  </React.StrictMode>
);
